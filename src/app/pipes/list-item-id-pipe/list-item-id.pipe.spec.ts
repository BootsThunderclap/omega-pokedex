import { ListItemIdPipe } from './list-item-id.pipe';

describe('ListItemIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ListItemIdPipe();
    expect(pipe).toBeTruthy();
  });
});
